services:
  memos:
    image: itwhale/memos:latest
    build:
      context: ..
      dockerfile: scripts/Dockerfile
    container_name: memos
    volumes:
      - ~/.memos/:/var/opt/memos
    ports:
      - 5230:5230
